<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="payment_provider_form" model="ir.ui.view">
        <field name="name">HDFC Provider Form</field>
        <field name="model">payment.provider</field>
        <field name="inherit_id" ref="payment.payment_provider_form"/>
        <field name="arch" type="xml">
            <group name="provider_credentials" position='inside'>
                <group invisible="code != 'hdfc'"
                       name="hdfc_credentials">
                       <field name="hdfc_merchant_id" required="code == 'hdfc' and state != 'disabled'" />
                       <field name="hdfc_merchant_name" required="code == 'hdfc' and state != 'disabled'" />
                       <field name="hdfc_merchant_key" required="code == 'hdfc' and state != 'disabled'" password="1"/>
                       <field name="hdfc_merchant_vpa" required="code == 'hdfc' and state != 'disabled'" />
                </group>
            </group>

            <group name="availability" position='inside' >
                <field name="show_qr_on_invoice" invisible="code != 'hdfc'" required="code == 'hdfc' and state != 'disabled'" />
            </group>
        </field>
    </record>

</odoo>
